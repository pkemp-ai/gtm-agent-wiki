# Outbox

Artifacts sent to Acme stakeholders — digests, ratification sheets, walkthroughs.
One dated file per delivery. Exempt from orphan and front-matter checks (SPEC §3).
This example is trimmed: the live digest is generated on demand by `scripts/digest.py`
rather than stored here. A real deployment keeps every send.
