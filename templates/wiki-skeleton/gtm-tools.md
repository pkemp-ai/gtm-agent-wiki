---
type: runbook
description: "TBD (write for retrieval): the GTM stack — what each tool is for, how agents access it, data flows between tools, what's broken"
owner: TBD
sources: []
update-cadence: monthly   # verification cadence
staleness-horizon: 90d
evidence-as-of:
last-verified: 2026-08-19
---

# GTM tools

*Runbook — verified by execution: every access pattern carries a `verified: <date>` stamp from actually running it; failures are marked **broken** with the error, never deleted (SPEC §8).*

## Stack

*What goes here: per tool — what it's for, who owns it, how agents access it (or "no agent access"), and a verified stamp. Deep per-tool usage guides fan out to `references/tool-<name>.md` only when an agent workflow genuinely depends on one.*

## Data flows

*What goes here: which tool feeds which — the map that explains why numbers disagree.*

## Broken / deprecated

*What goes here: tools going away and access that no longer works — entries move here rather than being deleted.*
